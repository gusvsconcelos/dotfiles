## Installation

### Arch Linux:

rxfetch is available in the AUR, you can install it with

```yaml
$ yay -S rxfetch
```

also, install the fonts too

```yaml
$ yay -S ttf-material-design-icons
```

then, copy/move the `rxfetch` file to `/usr/bin/`
